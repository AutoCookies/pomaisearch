# Pomai Search Engine

Pomai Search is an embeddable vector search engine focused on deterministic behavior, bounded execution, and explainable query flow.

See `pomai_search/README.md` and `pomai_search/docs/ENGINE_CONTRACT.md` for the normative behavior contract.
